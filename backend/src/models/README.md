# Models

This directory contains data models and schemas.

## Purpose
- Database models
- Data validation schemas
- Type definitions
- etc.

## Example
```javascript
// User.js
class User {
  constructor(id, name, email) {
    this.id = id;
    this.name = name;
    this.email = email;
  }
}

module.exports = User;
```
